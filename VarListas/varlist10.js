//10º Crie uma função que recebe um parâmetro e utiliza const para declarar uma variável local que armazena o dobro desse parâmetro.
function lista10(numero) {
    const dobra = numero * 2;
    console.log(dobra);
}
lista10(5);